extends layout

block content
	canvas#game(width=640, height=640)
	script(type='text/javascript').
		var star = !{JSON.stringify(star)};
		var ship = !{JSON.stringify(ship)};
		var user = !{JSON.stringify(user)};
		var game = new Game(star, ship, user);

